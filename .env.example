# Address of the external Ollama the container proxies /api/* to (host:port).
# Copy this file to .env and set the real value. .env is gitignored.
OLLAMA_UPSTREAM=your-ollama-host:11434

# Host port the game is served on (http://localhost:<WEB_PORT>). Defaults to 8080.
WEB_PORT=8080
